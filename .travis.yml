before_install:
  - sudo apt-get update
  - yes | sudo add-apt-repository ppa:cmrx64/cargo
  - yes | sudo add-apt-repository ppa:hansjorg/rust

install:
  - sudo apt-get install cargo rust-nightly libopenblas-dev

script:
  - rustc --version
  - make
  - make test

branches:
  only: master
